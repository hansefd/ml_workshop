Potrzebne biblioteki:

1. Pytorch:
    dla Windowsa: conda install pytorch torchvision cudatoolkit=10.1 -c pytorch
    dla macOS: conda install pytorch torchvision -c pytorch
2. Matplotlib:
    pip install matplotlib
3. TQDM:
    pip install tqdm